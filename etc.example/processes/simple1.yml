id: simple1
type: bidirectional
components:
  - id: in
    type: entry
    bean: basicEntry
    converters:
      out: jsonToDataConverter
    next: arraySplitter

  - id: arraySplitter
    type: transformer
    bean: dataSplitter
    next: adder

  - id: adder
    type: transformer
    bean: spelTransformer
    configuration:
      spel: "new Integer(content)+1"
    next: out

  - id: out
    type: sink
    bean: bidirectionalSink